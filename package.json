{
  "name": "portoakmal-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm --prefix ./my-portfolio run dev",
    "build": "npm --prefix ./my-portfolio run build",
    "lint": "npm --prefix ./my-portfolio run lint",
    "preview": "npm --prefix ./my-portfolio run preview"
  },
  "dependencies": {
    "emailjs-com": "^3.2.0"
  }
}
